# Copy to .env and adjust. Every value has a local-development default;
# set real credentials for any non-local deployment.
DB_NAME=fleet_telematics
DB_USER=fleet
DB_PASSWORD=change_me_outside_local_dev
DB_HOST_PORT=5435
BACKEND_HOST_PORT=8086
FRONTEND_HOST_PORT=3000
WS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
