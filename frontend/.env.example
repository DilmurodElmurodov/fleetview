# Backend origin. Leave EMPTY ("") to use the page's own origin (Docker/nginx proxy setup).
VITE_API_URL=http://localhost:8085

# UI-level gate for the staff-login modal on the landing page (NOT real security)
VITE_STAFF_ACCESS_CODE=fleet2026
